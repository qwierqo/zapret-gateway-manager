# API

This directory is reserved for the future local API layer.

The API is expected to expose gateway state and basic control actions, including:

- current backend status;
- active profile;
- available profiles;
- last service check results;
- manual check triggers;
- future profile apply actions.

At the current stage, this directory exists only to document the intended architecture.